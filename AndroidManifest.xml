<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.shoque"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.saxion.shoque.SeaShoque"
            android:label="@string/title_activity_start_menu"
            android:theme="@android:style/Theme.NoTitleBar" 
            android:screenOrientation="sensor">
            	
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity 
            android:name="com.saxion.shoque.GameActivity"
            android:label="@string/title_activity_start_menu"
            android:theme="@android:style/Theme.NoTitleBar"
            android:screenOrientation="sensorLandscape" >
            </activity>
        
         <activity 
            android:name="com.saxion.shoque.SetUpActivity"
            android:label="@string/title_activity_start_menu"
            android:theme="@android:style/Theme.NoTitleBar"
            android:screenOrientation="sensorLandscape" >
            </activity>
    </application>

</manifest>
